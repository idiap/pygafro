# SPDX-FileCopyrightText: Copyright © 2024 Idiap Research Institute <contact@idiap.ch>
#
# SPDX-FileContributor: Philip Abbet <philip.abbet@idiap.ch>
#
# SPDX-License-Identifier: MPL-2.0


name: Publish
run-name: Publish package on PyPI

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish-source-on-pypi:
    name: Publish source package on PyPI

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'true'

    - name: Install the Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install twine build

    - name: Build the source package
      run: python3 -m build --sdist

    - name: Upload to PyPI
      run: twine upload --skip-existing dist/pygafro-*.tar.gz
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_KEY }}

  publish-assets-on-pypi:
    name: Publish assets package on PyPI

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'true'

    - name: Install the Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install twine==6.0.1 build

    - name: Build the assets package
      run: |
        git clone https://github.com/idiap/gafro_robot_descriptions.git
        cd gafro_robot_descriptions
        git checkout 1444fca9fc8a04103b52e26ca8f4a2512d0d2299
        cd ../assets
        mkdir -p source
        cp -r ../gafro_robot_descriptions/assets source/.
        find source -name "*.yaml" -delete
        python3 -m build --sdist

    - name: Upload to PyPI
      run: twine upload --skip-existing assets/dist/pygafro_assets-*.tar.gz
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_PYGAFRO_ASSETS_API_KEY }}

  publish-wheels-on-pypi:
    name: Publish ${{ matrix.os }} / py${{ matrix.python }} wheels on PyPI

    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, ubuntu-24.04-arm, macos-15]
        python: [38, 39, 310, 311, 312, 313]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13' 

      - name: Install Python tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install cibuildwheel==3.0.1 twine

      - name: Build wheels
        run: python -m cibuildwheel --output-dir wheelhouse
        env:
          CIBW_BUILD: cp${{ matrix.python }}-*

      - name: Upload to PyPI
        run: twine upload --skip-existing wheelhouse/pygafro-*.whl
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_KEY }}
